{
  "name": "666myidjshoutcaststream-worker",
  "version": "1.3.0",
  "description": "Cloudflare Worker API layer for 666RadioCoreDJ: health, nowplaying and stream preset endpoints.",
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "check": "node --check src/index.js"
  },
  "license": "MIT",
  "devDependencies": {}
}
