# Incident Response

Secret leak: stop deploy, revoke token, rotate secret, remove value, audit history. Bot failure: check host logs, permissions, env streams, npm check, restart. Worker failure: verify Root Directory worker, run worker check, inspect Cloudflare logs, rollback if needed.

