# Codex Next Steps

1. Verify Cloudflare Root Directory = worker.
2. Configure discord-bot/.env outside Git.
3. Register slash commands with real Discord env.
4. Start Bot on 24/7 Node host.
5. Configure RADIO_LOG_CHANNEL_ID if track/health events should be posted to Discord.
6. Test /play, /pause, /resume, /stop, /volume, /radio panel/status.
7. Verify ICY StreamTitle appears in /radio status and track-change log posts.
8. Verify stream health transitions with a real SHOUTcast/Icecast URL.
9. Capture SonicPanel Skip/Jingle endpoints without secrets.
10. Deploy Worker and test /health, /presets, /nowplaying.

