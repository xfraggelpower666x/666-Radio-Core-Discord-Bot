# Codex Next Steps

1. Verify Cloudflare Root Directory = worker.
2. Configure discord-bot/.env outside Git.
3. Register slash commands with real Discord env.
4. Start Bot on 24/7 Node host.
5. Test /play, /pause, /resume, /stop, /volume, /radio panel/status.
6. Capture SonicPanel Skip/Jingle endpoints without secrets.
7. Deploy Worker and test /health, /presets, /nowplaying.

