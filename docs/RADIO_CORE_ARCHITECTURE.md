# Radio Core Architecture

Purpose: Discord voice playback for configured stream presets. Non-purpose: AutoDJ provider ownership or Worker runtime. Primary/backup stream URLs are SECRET_REF / NACHZUTRAGEN. NowPlaying source is Worker SHOUTCAST_PUBLIC_STATUS_URL when configured. Listener source and DJ-name source are UNBEKANNT / NACHZUTRAGEN.

