# Environment Variables

Discord Bot values are in discord-bot/.env.example as placeholders only. Worker values are in worker/.dev.vars.example and worker/wrangler.toml. Secrets must live in host/GitHub/Cloudflare secret stores, never in Git.

