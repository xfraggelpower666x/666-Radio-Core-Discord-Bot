# Worker Route Map

GET /health PASS. GET /nowplaying PASS / config-dependent. GET /presets PASS. GET /preset/1..5 PASS. Planned /stream/status, /discord/*, /autodj/* and /remote/* are NOT ACTIVE and must be authenticated before production.

