# Stream Source Map

Preset URLs 1..5 are configured via env and treated as SECRET_REF unless PUBLIC_PRESET_URLS is explicitly true. SHOUTCAST_PUBLIC_STATUS_URL is NACHZUTRAGEN for Worker /nowplaying.

