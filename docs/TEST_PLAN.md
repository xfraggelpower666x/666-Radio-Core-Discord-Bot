# Test Plan

Completed: discord-bot npm install PASS, npm run check PASS, npm run audit:local PASS, worker npm install PASS, worker npm run check PASS. Runtime smoke tests require real Discord/Cloudflare/SonicPanel env values outside Git.

