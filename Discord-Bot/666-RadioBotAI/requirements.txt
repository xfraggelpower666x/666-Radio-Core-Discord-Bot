# 666 RadioBotAI add-on modules (cogs + helpers).
#
# These files are meant to be dropped into a Vocard fork that already provides
# the in-tree `voicelink` package and its dependencies. `voicelink` is NOT a
# PyPI package, so it is intentionally absent here; radiobotai_voicelink_compat
# falls back to env-driven defaults when the Vocard host is not present.
#
# The packages below are the third-party imports the add-on code actually uses.
discord.py[voice]==2.4.0
aiohttp==3.13.4
PyNaCl==1.6.2
